import Foundation

struct Post {
    var id: Int
    var userId: Int
    var courseId: Int
    var categoryId: Int
    var title: String
    var content: String
    var createdAt: Date
    var updatedAt: Date
}
